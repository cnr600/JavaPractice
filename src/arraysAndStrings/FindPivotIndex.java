package arraysAndStrings;

public class FindPivotIndex {

	public int pivotIndex(int[] nums) {
		int leftSum, pivot,	rightSum, totalSum=0;
		if(nums.length<3) return -1;
		for(int i=0;i<nums.length;i++) {
			totalSum+=nums[i];
		}
		
		
		return 0;
	}
	
	
}
